/*const products = await ProductsServices.getProducts

 const {id,name,email,password} = req.body; {
   try{
    insert({
    
        name:name,
        id:id,
        email:email,
        password:password

   }) 
   
   
   }catch(e){

   return;

   }


};*/